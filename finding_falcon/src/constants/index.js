import { planetImages, spaceShipImages } from './images'
import { END_POINT, PLANET_PATH, VEHICLE_PATH, TOKEN_PATH, FIND_FALCON_PATH } from './url'

export {
    planetImages,
    spaceShipImages,
    END_POINT,
    PLANET_PATH,
    VEHICLE_PATH,
    TOKEN_PATH,
    FIND_FALCON_PATH
}